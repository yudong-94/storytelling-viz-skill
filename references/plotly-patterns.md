# Shared Plotly Patterns

Use these defaults unless the dataset needs something more specific:

- prominent title and supporting subtitle above the chart area
- concise source line outside the plotting area
- value-first hover templates
- stable margins for long labels
- responsive resize handling
- postMessage height reporting when the chart will later be embedded
- a short fallback summary below the chart
